import { CoachRolePipe } from './coach-role.pipe';

describe('CoachRolePipe', () => {
  it('create an instance', () => {
    const pipe = new CoachRolePipe();
    expect(pipe).toBeTruthy();
  });
});
